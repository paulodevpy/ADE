<Window x:Class="ADE.UI.SelectMonitorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecionar Monitor"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Grid.ColumnSpan="2"
                   FontWeight="Bold" 
                   FontSize="14" 
                   Margin="0,0,0,10"
                   Text="Selecione o monitor para captura:"/>

        <ListBox
            Grid.Row="1"
            Name="MonitorListBox" 
            DisplayMemberPath="Label"
            SelectionChanged="MonitorListBox_SelectionChanged"/>

        <Border Grid.Row="1" 
                Grid.Column="1" 
                BorderBrush="Gray" 
                BorderThickness="1" 
                Margin="10,0,0,0"
                Background="LightGray">
            <Grid>
                <TextBlock Text="Pré-visualização" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="5"/>
                <Image Name="PreviewImage" 
                       Margin="10,30,10,10"
                       Stretch="Uniform"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" 
                    Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Width="100" 
                    Height="35" 
                    Margin="0,0,10,0" 
                    Content="Cancelar" 
                    Click="Cancelar_Click"/>
            <Button Width="100" 
                    Height="35" 
                    Content="Selecionar" 
                    Click="Selecionar_Click"/>
        </StackPanel>

    </Grid>

</Window>
